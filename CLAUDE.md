# Agent Preferences

## Package Manager
- Use `bun` for all package installations and script execution

## Database Schema
- To make modifications to database schema -- first modify schema.ts, then run bun db:generate and tell me to run the migration. do NOT create the sql file yourself
